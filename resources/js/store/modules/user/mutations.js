export default {
    appId: (state, app_id) => state.app_id = app_id,
    user: (state, user) => state.user = user,
    accessToken: (state, access_token) => state.access_token = access_token,
    friends: (state, friends) => state.friends = friends,
}
